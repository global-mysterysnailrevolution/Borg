[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tavily-mcp"
version = "0.1.0"
description = "MCP server for Tavily — auto-generated by HackForge"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0",
    "httpx>=0.27",
    "pydantic>=2.0",
]

[project.scripts]
serve = "server:main"
